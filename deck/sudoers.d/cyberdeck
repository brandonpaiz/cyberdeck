# Lets the deck user power the machine off or reboot it from a ROM
# (the bridge's POWER frame). Installed by deck/setup.sh with mode 0440.
deck ALL=(root) NOPASSWD: /usr/bin/systemctl poweroff, /usr/bin/systemctl reboot
